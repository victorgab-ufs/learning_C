#include <stdio.h>

int main(){
  
  // laço controlado por uma condição de parada (while -> enquanto)
  int i = 1;  // inicialização
  while (i <= 5) { 
    printf("%d\n", i); // condição
    i++; // incremento (essencial)
  }
  
  return 0;
}
